# Calculate root mean squared error

Calculate root mean squared error

## Usage

``` r
calc_rmse(pred_matrix, obs_matrix)
```

## Arguments

- pred_matrix:

  Predicted values matrix (time x space)

- obs_matrix:

  Observed values matrix (time x space)

## Value

Scalar RMSE value
