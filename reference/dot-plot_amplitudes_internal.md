# Internal function for amplitude plotting

Internal function for amplitude plotting

## Usage

``` r
.plot_amplitudes_internal(
  x,
  scale = "standardized",
  scale_y = "fixed",
  events = NULL,
  layout = NULL
)
```
