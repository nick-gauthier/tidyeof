% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_cca.R
\name{prep_cv_folds}
\alias{prep_cv_folds}
\title{Prepare cross-validation folds for CCA tuning}
\usage{
prep_cv_folds(
  predictor,
  response,
  kfolds = 5,
  max_k_pred = 10,
  max_k_resp = 10,
  scale = FALSE,
  rotate = FALSE,
  monthly = FALSE,
  weight = TRUE
)
}
\arguments{
\item{predictor}{A stars object with predictor data}

\item{response}{A stars object with response data}

\item{kfolds}{Number of cross-validation folds (default 5)}

\item{max_k_pred}{Maximum number of predictor EOFs to compute (default 10)}

\item{max_k_resp}{Maximum number of response EOFs to compute (default 10)}

\item{scale}{Logical, whether to scale data before EOF extraction (default FALSE)}

\item{rotate}{Logical, whether to apply varimax rotation (default FALSE)}

\item{monthly}{Logical, whether to compute monthly climatology (default FALSE)}

\item{weight}{Logical, whether to apply area weighting (default TRUE)}
}
\value{
A cv_folds S3 object containing:
  \item{folds}{List of fold data, each containing train patterns and test data}
  \item{max_k_pred}{Maximum predictor k used}
  \item{max_k_resp}{Maximum response k used}
  \item{kfolds}{Number of folds}
  \item{common_times}{Vector of overlapping time steps}
  \item{pattern_opts}{List of pattern extraction options}
}
\description{
Computes EOF patterns at maximum k once per fold, enabling cheap truncation
during grid search. This is the expensive step - call it once, then use
`tune_cca()` for fast hyperparameter exploration.
}
\examples{
\dontrun{
# Prepare folds (expensive, run once)
cv <- prep_cv_folds(coarse_data, fine_data,
                    kfolds = 5, max_k_pred = 10, max_k_resp = 10)

# Then tune over k_pred and k_resp
results <- tune_cca(cv, k_pred = 1:10, k_resp = 1:10)
}
}
