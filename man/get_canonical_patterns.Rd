% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/couple.R
\name{get_canonical_patterns}
\alias{get_canonical_patterns}
\title{Get Canonical Spatial Patterns from Coupled Patterns}
\usage{
get_canonical_patterns(object, type = c("predictor", "response"), k = NULL)
}
\arguments{
\item{object}{A coupled_patterns object}

\item{type}{Either "predictor" or "response"}

\item{k}{Number of canonical modes to extract (default: all available)}
}
\value{
A stars object with canonical spatial patterns (dimension "CV" instead of "PC")
}
\description{
Computes the spatial patterns corresponding to each canonical mode by
taking linear combinations of the original EOFs weighted by CCA coefficients.
These are the spatial patterns that, when projected onto the data, yield
the canonical variates.
}
\examples{
\dontrun{
coupled <- couple(pred_patterns, resp_patterns, k = 3)

# Get canonical patterns for response side
resp_canonical <- get_canonical_patterns(coupled, type = "response")
plot(resp_canonical)

# Compare to original EOFs
plot(coupled$response_patterns$eofs)
}
}
