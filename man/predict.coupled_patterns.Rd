% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/couple.R
\name{predict.coupled_patterns}
\alias{predict.coupled_patterns}
\title{Predict Method for Coupled Patterns}
\usage{
\method{predict}{coupled_patterns}(object, newdata, k = NULL, reconstruct = TRUE, nonneg = "auto", ...)
}
\arguments{
\item{object}{A coupled_patterns object from couple()}

\item{newdata}{New predictor data (stars object) for making predictions}

\item{k}{Number of CCA modes to use for prediction. If NULL, uses all available modes}

\item{reconstruct}{Logical, whether to reconstruct the full spatial field (default: TRUE)}

\item{nonneg}{How to handle non-negative constraints: "auto", TRUE, FALSE, or "inherit"}

\item{...}{Additional arguments (currently unused)}
}
\value{
If reconstruct=TRUE, returns a stars object with reconstructed spatial fields.
        If reconstruct=FALSE, returns a tibble with predicted amplitudes.
}
\description{
This function makes predictions using a coupled_patterns object created by couple().
It applies the learned CCA relationship to new predictor data to predict response patterns.
}
\examples{
\dontrun{
# Create coupled patterns
coupled <- couple(pred_patterns, resp_patterns, k = 3)

# Make predictions on new data
predictions <- predict(coupled, new_predictor_data)

# Just get predicted amplitudes without spatial reconstruction
amplitudes <- predict(coupled, new_predictor_data, reconstruct = FALSE)
}
}
